منتقل‌شده به فرایند شماره ۱ — مرحلهٔ ۵۸ (Stub)
================================================

۱. وضعیت
---------
این رکورد صرفاً برای **حفظ شمارهٔ مرحلهٔ ۵۸** در فهرست SOP و شفاف‌سازی سوابق است: محتوای عملیاتیِ مربوط به این شمارهٔ قدیمی، **به فرایند شمارهٔ ۱** منتقل شده است.

۲. فرایند هدف (شمارهٔ ۱)
-------------------------
**کد:** `educational_leave`  
**عنوان:** مرخصی آموزشی موقت از ثبت‌نام در کلاس‌ها  

برای دستورالعمل، فلوچارت و اجرا به رجیستری و SOP همان فرایند مراجعه کنید:

  `metadata/process_registry/processes/educational_leave/SOP_document.txt`  
  `metadata/process_registry/processes/educational_leave/SOP_flowchart.png`

۳. یادداشت فنی
---------------
این فرایند در متادیتا بدون ترنزیشن تعریف شده است (`process_merged_to_one` — حالت واحد `merged`). کاربرد عملیاتی جداگانه‌ای ندارد؛ هدف، **ارجاع** به فرایند ۱ است.

همگام‌سازی متن Stub با دیتابیس (در صورت نیاز):
  python scripts/sync_sop_doc_from_registry_files.py --code process_merged_to_one
