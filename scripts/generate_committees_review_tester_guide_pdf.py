#!/usr/bin/env python3
"""
راهنمای PDF آزمایش — فرایند ۱۳ بخش ۳ زیرفرایند ب
«بررسی کمیته‌های نظارت و آموزش» (قطع زودرس درمان آموزشی).

بدون اصطلاحات فنی؛ برای اپراتور و کاربر آزمایشی.

اجرا از ریشهٔ ریپو:
  python scripts/generate_committees_review_tester_guide_pdf.py

خروجی:
  docs/راهنمای_تست_فرایند_۱۳_کمیته_نظارت_و_آموزش.pdf
"""
from __future__ import annotations

import sys
from pathlib import Path

if sys.platform == "win32" and hasattr(sys.stdout, "reconfigure"):
    try:
        sys.stdout.reconfigure(encoding="utf-8")
    except Exception:
        pass

ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(ROOT))
sys.path.insert(0, str(ROOT / "scripts"))

from generate_ui_tester_guide_pdf import (  # noqa: E402
    PdfBuilder,
    _heading_style,
    _fa,
)
from fpdf.enums import Align, TableBordersLayout, TableCellFillMode

OUT_PDF = ROOT / "docs" / "راهنمای_تست_فرایند_۱۳_کمیته_نظارت_و_آموزش.pdf"

ACCOUNTS = [
    ("کمیته نظارت", "supervision_committee1", "demo123"),
    ("کمیته آموزش", "education_committee1", "demo123"),
    ("معاون آموزش (همان پنل کمیته آموزش)", "deputy_education1", "demo123"),
    ("درمانگر آموزشی (برای آغاز مسیر کامل)", "therapist1", "demo123"),
    ("دانشجو (دوره جامع — درمان آغاز شده)", "student2", "demo123"),
    ("مدیر سامانه (در صورت نیاز)", "admin", "admin123"),
]

SCENARIO_SUPERVISION = {
    "letter": "۱",
    "title": "ثبت پیشنهاد کمیته نظارت",
    "who": "کمیته نظارت (supervision_committee1)",
    "steps": [
        "با حساب supervision_committee1 وارد شوید.",
        "به پنل کمیته نظارت بروید (پس از ورود معمولاً تب «کارهای من» باز می‌شود).",
        "در فهرست «کارهای من»، پروندهٔ «بررسی کمیته‌های نظارت و آموزش» را پیدا کنید.",
        "روی پرونده کلیک کنید تا جزئیات باز شود.",
        "باکس راهنمای «بررسی کمیته‌ها (فرایند ۱۳ — بخش ۳ زیرفرایند ب)» را ببینید.",
        "نوار سه‌مرحله‌ای بالا باید مرحلهٔ «کمیته نظارت» را فعال (آبی) نشان دهد.",
        "در بخش «پرونده ورودی» این موارد را بخوانید: منبع ورود، علت قطع درمانگر، "
        "توضیحات درمانگر، و در صورت وجود نظر کمیسیون تخصصی.",
        "پیام زرد راهنما را بخوانید: کمیته نظارت فقط پیشنهاد مشورتی ثبت می‌کند.",
        "به پایین صفحه بروید و فرم «پیشنهاد کمیته نظارت» را باز کنید.",
        "یکی از دو گزینه را انتخاب کنید: «ادامه آموزش» یا «قطع آموزش».",
        "توضیحات و مستندات پیشنهاد را بنویسید (اجباری).",
        "دکمه «ثبت اطلاعات این مرحله» را بزنید.",
        "سپس در بخش «تصمیم کمیته»، دکمه «ثبت پیشنهاد» را بزنید.",
        "صفحه را تازه کنید (F5).",
    ],
    "expect": [
        "وضعیت به «تصمیم‌گیری کمیته آموزش» تغییر کند.",
        "نوار مراحل، مرحلهٔ دوم (کمیته آموزش) را فعال نشان دهد.",
        "پیشنهاد ثبت‌شده در پرونده قابل مشاهده باشد.",
        "اگر فرم را پر نکرده باشید، با زدن دکمه تصمیم پیام فارسی راهنما ببینید.",
    ],
    "checks": [
        "پرونده در «کارهای من» کمیته نظارت دیده شد",
        "باکس راهنما و نوار مراحل نمایش داده شد",
        "پرونده ورودی (علت قطع و منبع) خوانا بود",
        "فرم پیشنهاد با دو گزینه کار کرد",
        "ثبت فرم و سپس «ثبت پیشنهاد» بدون خطا بود",
        "وضعیت به مرحله کمیته آموزش رفت",
        "متن یا دکمه گیج‌کننده بود",
    ],
}

SCENARIO_EDUCATION_CONTINUE = {
    "letter": "۲",
    "title": "حکم ادامه آموزش و مهلت ۵ روزه دانشجو",
    "who": "کمیته آموزش (education_committee1) و سپس دانشجو (student2)",
    "steps": [
        "خارج شوید. با education_committee1 (یا deputy_education1) وارد شوید.",
        "به پنل کمیته آموزش — تب «کارهای من» بروید.",
        "همان پرونده را باز کنید (وضعیت: تصمیم‌گیری کمیته آموزش).",
        "باکس راهنما: مرحلهٔ «کمیته آموزش» فعال است.",
        "پیشنهاد ثبت‌شده کمیته نظارت را در پرونده ببینید.",
        "فرم «حکم نهایی کمیته آموزش» را باز کنید.",
        "فیلدهای فقط‌خواندنی (علت قطع، نظر کمیسیون، پیشنهاد نظارت) را بررسی کنید.",
        "در صورت تمایل یادداشت جلسه بنویسید (اختیاری).",
        "فرم را ثبت کنید.",
        "در بخش «تصمیم کمیته»، دکمهٔ سبز «ادامه آموزش» را بزنید.",
        "صفحه را تازه کنید — وضعیت باید «در انتظار بازگشت به درمان» باشد.",
        "خارج شوید. با student2 وارد پورتال دانشجو شوید.",
        "کارت یا بخش «آغاز دوباره درمان آموزشی» را ببینید.",
        "متن پیامک استاندارد و مهلت ۵ روزه را بخوانید.",
        "دکمه «آغاز فرایند تغییر/آغاز دوباره درمان» را بزنید.",
    ],
    "expect": [
        "دکمه «ادامه آموزش» سبز و قابل تشخیص باشد.",
        "پس از حکم ادامه، دانشجو باکس مهلت ۵ روزه را ببیند.",
        "دانشجو بتواند فرایند آغاز دوباره درمان را شروع کند.",
        "پیام فارسی واضح دربارهٔ انتخاب درمانگر جدید نمایش داده شود.",
    ],
    "checks": [
        "کمیته آموزش پرونده را در «کارهای من» دید",
        "پیشنهاد نظارت در فرم حکم نمایش داده شد",
        "دکمه «ادامه آموزش» سبز بود و کار کرد",
        "وضعیت «در انتظار بازگشت به درمان» ثبت شد",
        "دانشجو باکس مهلت ۵ روزه را دید",
        "دکمه آغاز دوباره درمان کار کرد",
        "متن یا دکمه گیج‌کننده بود",
    ],
}

SCENARIO_EDUCATION_TERMINATE = {
    "letter": "۳",
    "title": "حکم قطع قطعی آموزش (اخراج)",
    "who": "کمیته آموزش (education_committee1) — پروندهٔ جداگانه",
    "steps": [
        "از مسئول پروژه بخواهید یک پروندهٔ آزمایشی دیگر در مرحله «تصمیم‌گیری کمیته آموزش» "
        "آماده کند (یا مسیر سناریوی ۱ را دوباره طی کنید).",
        "با education_committee1 وارد شوید و پرونده را باز کنید.",
        "فرم «حکم نهایی کمیته آموزش» را تکمیل و ثبت کنید.",
        "در بخش «تصمیم کمیته»، دکمهٔ قرمز «قطع قطعی» (یا مشابه) را بزنید.",
        "صفحه را تازه کنید.",
        "باکس قرمز «حکم قطع قطعی صادر شد» را در پنل راهنما ببینید.",
        "متن را بخوانید: غیرفعال‌سازی حساب، لغو جلسات، نامه رسمی، و در صورت انترن بودن ارجاع بیماران.",
    ],
    "expect": [
        "وضعیت نهایی «قطع قطعی آموزش (اخراج)» باشد.",
        "پیام قرمز راهنما در پنل نمایش داده شود.",
        "فرایند به‌عنوان تکمیل‌شده علامت بخورد.",
    ],
    "checks": [
        "دکمه «قطع قطعی» قرمز و قابل تشخیص بود",
        "ثبت حکم بدون خطا انجام شد",
        "وضعیت نهایی «قطع قطعی آموزش» بود",
        "پیام راهنمای قرمز نمایش داده شد",
        "متن یا دکمه گیج‌کننده بود",
    ],
}


class CommitteesReviewGuide(PdfBuilder):
    def cover(self) -> None:
        p = self.pdf
        p.add_page()
        p.set_font("Vazir", "B", 15)
        p.ln(8)
        p.cell(0, 12, _fa("راهنمای آزمایش و استفاده"), align="C", new_x="LMARGIN", new_y="NEXT")
        p.set_font("Vazir", "B", 12)
        p.cell(
            0,
            10,
            _fa("فرایند ۱۳ — بخش ۳ زیرفرایند ب"),
            align="C",
            new_x="LMARGIN",
            new_y="NEXT",
        )
        p.set_font("Vazir", "B", 11)
        p.cell(
            0,
            9,
            _fa("«بررسی کمیته‌های نظارت و آموزش»"),
            align="C",
            new_x="LMARGIN",
            new_y="NEXT",
        )
        p.set_font("Vazir", "", 9.5)
        p.cell(
            0,
            8,
            _fa("(قطع زودرس درمان آموزشی — مسیر انضباطی یا ارجاع از کمیسیون تخصصی)"),
            align="C",
            new_x="LMARGIN",
            new_y="NEXT",
        )
        p.ln(4)
        p.set_font("Vazir", "", 9.5)
        p.multi_cell(
            0,
            6,
            _fa(
                "این کتابچه برای اپراتور تست، کارشناس پذیرش و اعضای کمیته "
                "نوشته شده است. هدف آن است بدون نیاز به دانش فنی، بتوانید "
                "رابط کاربری این فرایند را امتحان کنید و مطمئن شوید درست کار می‌کند."
            ),
            align="R",
        )
        p.ln(6)
        for label, blank in [
            ("نام اپراتور تست", "________________________"),
            ("تاریخ آزمایش", "________________________"),
            ("آدرس سایت (از مسئول پروژه)", "________________________"),
        ]:
            p.cell(0, 8, _fa(f"{label}: {blank}"), align="R", new_x="LMARGIN", new_y="NEXT")
        p.ln(3)
        self._warn_box(
            "قبل از شروع، سامانه باید روشن باشد. "
            "اگر پروندهٔ آزمایشی آماده نیست، از مسئول پروژه بخواهید "
            "یک نمونه در مرحله «بررسی کمیته نظارت» برای شما بسازد."
        )

    def intro(self) -> None:
        self.pdf.add_page()
        self._section_bar("۱ — این فرایند چیست؟")
        self._body(
            "وقتی درمانگر آموزشی گزارش می‌دهد دانشجو مناسب درمان تحلیلی نیست "
            "(علت ۴ — مسیر انضباطی)، یا وقتی کمیسیون تخصصی صلاحیت دانشجو را رد می‌کند، "
            "پرونده به «کمیته‌های نظارت و آموزش» ارجاع می‌شود. "
            "این زیرفرایند در دو مرحله تصمیم می‌گیرد:"
        )
        self._numbered_list([
            "کمیته نظارت (حداکثر ۳ روز): پیشنهاد مشورتی «ادامه آموزش» یا «قطع آموزش»",
            "کمیته آموزش (حداکثر ۶ روز): حکم نهایی — ادامه یا قطع قطعی",
        ])
        self._body(
            "اگر حکم «ادامه» باشد، دانشجو ۵ روز فرصت دارد درمان را از سر بگیرد. "
            "اگر حکم «قطع» باشد، آموزش به‌صورت قطعی پایان می‌یابد."
        )
        self._section_bar("۲ — چه کسانی در آزمایش نقش دارند؟")
        self._bullet_list([
            "کمیته نظارت: بررسی پرونده و ثبت پیشنهاد مشورتی",
            "کمیته آموزش: صدور حکم نهایی (ادامه یا قطع)",
            "دانشجو: در صورت حکم ادامه، آغاز دوباره درمان در مهلت ۵ روز",
            "درمانگر (اختیاری): برای آزمایش مسیر کامل از ابتدا با علت ۴",
            "اپراتور تست: هماهنگی، بررسی پرونده‌ها و ثبت نتیجه",
        ])

    def prerequisites(self) -> None:
        self.pdf.add_page()
        self._section_bar("۳ — قبل از تست چه باید آماده باشد؟")
        self._body("اپراتور این موارد را یک‌بار بررسی می‌کند:", bold=True)
        self._check_table([
            "سامانه باز می‌شود و صفحهٔ ورود دیده می‌شود",
            "حساب‌های آزمایشی کمیته نظارت و کمیته آموزش در دسترس است",
            "یک پروندهٔ «بررسی کمیته‌های نظارت و آموزش» در مرحله مناسب وجود دارد",
            "در پرونده، علت قطع درمان و منبع ورود (مسیر انضباطی یا ارجاع کمیسیون) مشخص است",
            "برای سناریوی دانشجو: حساب student2 با درمان آغاز‌شده آماده است",
        ])
        self._tip_box(
            "اگر پروندهٔ آماده ندارید، از مسئول پروژه بخواهید یک نمونه بسازد. "
            "یا با therapist1 فرایند «قطع زودرس درمان» را با علت ۴ آغاز کنید "
            "تا زیرفرایند کمیته‌ها خودکار باز شود."
        )

    def login_and_accounts(self) -> None:
        self.pdf.add_page()
        self._section_bar("۴ — ورود به سامانه")
        self._numbered_list([
            "مرورگر (Chrome یا Edge) را باز کنید.",
            "آدرس ورود را از مسئول پروژه بگیرید (معمولاً .../login).",
            "روی «ورود با رمز عبور» بزنید.",
            "سؤال ریاضی ساده را جواب دهید.",
            "نام کاربری و رمز را وارد کنید و «ورود» بزنید.",
            "برای عوض کردن نقش: «خروج» کنید و با حساب دیگر وارد شوید.",
        ])
        self._section_bar("۵ — حساب‌های آزمایشی")
        p = self.pdf
        with p.table(
            col_widths=(72, 58, 28),
            width=p.epw,
            text_align=Align.R,
            line_height=5.5,
            borders_layout=TableBordersLayout.ALL,
            cell_fill_color=(249, 250, 251),
            cell_fill_mode=TableCellFillMode.ROWS,
            padding=1.5,
        ) as table:
            hr = table.row()
            for h in [_fa("نقش"), _fa("نام کاربری"), _fa("رمز")]:
                hr.cell(h, align=Align.C, style=_heading_style())
            for role, user, pw in ACCOUNTS:
                row = table.row()
                row.cell(_fa(role))
                row.cell(user, align=Align.C)
                row.cell(pw, align=Align.C)
        p.ln(3)

    def ui_description(self) -> None:
        self.pdf.add_page()
        self._section_bar("۶ — صفحهٔ کمیته چه شکلی است؟")
        self._body("پس از ورود با حساب کمیته، به «پنل کمیته» می‌روید:", bold=True)
        self._bullet_list([
            "کمیته نظارت: /panel/portal/committee/supervision",
            "کمیته آموزش: /panel/portal/committee/education",
            "(آدرس دقیق را مسئول پروژه می‌دهد؛ معمولاً پس از ورود خودکار باز می‌شود)",
        ])
        self._body("در تب «کارهای من»، پرونده را باز کنید. باید این‌ها را ببینید:", bold=True)
        self._numbered_list([
            "عنوان: «بررسی کمیته‌های نظارت و آموزش (زیرفرایند ب)»",
            "باکس راهنما با عنوان «بررسی کمیته‌ها (فرایند ۱۳ — بخش ۳ زیرفرایند ب)»",
            "نوار سه‌مرحله‌ای: کمیته نظارت (۳ روز) — کمیته آموزش (۶ روز) — مهلت دانشجو (۵ روز)",
            "بخش «پرونده ورودی»: منبع ورود، علت قطع، توضیحات درمانگر، نظر کمیسیون",
            "پیام راهنمای مرحله (زرد یا آبی بسته به مرحله)",
            "نمایش مهلت (SLA) با تعداد روز باقی‌مانده",
            "فرم مرحله (پیشنهاد نظارت یا حکم آموزش)",
            "بخش «تصمیم کمیته» با دکمه‌های رنگی",
        ])
        self._warn_box(
            "قبل از زدن دکمهٔ تصمیم، حتماً فرم مرحله را پر و «ثبت» کنید. "
            "در غیر این صورت پیام فارسی می‌بینید که ابتدا فرم را تکمیل کنید."
        )
        self._section_bar("۷ — صفحهٔ دانشجو (پس از حکم ادامه)")
        self._bullet_list([
            "باکس «آغاز دوباره درمان آموزشی» با مهلت ۵ روز",
            "متن پیامک استاندارد دربارهٔ انتخاب درمانگر جدید",
            "دکمه «آغاز فرایند تغییر/آغاز دوباره درمان»",
        ])

    def operator_prep(self) -> None:
        self.pdf.add_page()
        self._section_bar("۸ — کارهای اپراتور قبل از آزمایش")
        self._numbered_list([
            "با admin یا از مسئول پروژه بپرسید یک پروندهٔ آزمایشی آماده باشد.",
            "یادداشت کنید: نام دانشجو، منبع ورود (علت ۴ یا ارجاع کمیسیون)، مرحلهٔ فعلی.",
            "با supervision_committee1 وارد شوید و مطمئن شوید پرونده در «کارهای من» است.",
            "اگر پرونده نیست، از مسئول پروژه بخواهید فرایند را برای دانشجوی آزمایشی آغاز کند.",
            "برای سناریوی کامل از ابتدا: therapist1 علت ۴ را ثبت کند و منتظر بمانید "
            "تا پرونده در صندوق کمیته نظارت ظاهر شود.",
        ])
        self._tip_box(
            "کمیته نظارت فقط «پیشنهاد» می‌دهد؛ حکم نهایی با کمیته آموزش است. "
            "این تفاوت در متن راهنمای صفحه نوشته شده."
        )

    def scenario(self, sc: dict) -> None:
        self.pdf.add_page()
        self._section_bar(f"سناریوی {sc['letter']} — {sc['title']}")
        self._body("با چه حساب‌هایی؟", bold=True)
        self._body(sc["who"])
        self._body("قدم‌به‌قدم:", bold=True)
        self._numbered_list(sc["steps"])
        self._body("باید چه ببینید؟ (نشانهٔ درست بودن)", bold=True)
        self._bullet_list(sc["expect"])
        self._body("چک‌لیست — بعد از انجام علامت بزنید:", bold=True)
        self._check_table(sc["checks"])
        self._blank_lines("مشکل یا باگ (چه انتظار داشتید / چه شد):", 2)

    def operator_verify(self) -> None:
        self.pdf.add_page()
        self._section_bar("۹ — بررسی اپراتور پس از تست")
        self._body("بعد از هر سناریو، این موارد را کنترل کنید:")
        self._check_table([
            "پرونده در «کارهای من» نقش درست ظاهر شد",
            "نوار مراحل وضعیت فعلی را درست نشان داد",
            "پرونده ورودی (علت، منبع، نظر کمیسیون) کامل بود",
            "فرم مرحله قبل از تصمیم اجباراً پر شد",
            "دکمهٔ ادامه سبز و دکمهٔ قطع قرمز بود",
            "وضعیت نهایی با انتظار سناریو یکی بود",
            "دانشجو (در مسیر ادامه) باکس مهلت ۵ روزه را دید",
            "متن‌های فارسی خوانا و بدون غلط جدی بودند",
        ])

    def quick_demo(self) -> None:
        self.pdf.add_page()
        self._section_bar("۱۰ — مسیر سریع دمو (حدود ۲۰ دقیقه)")
        self._numbered_list([
            "اپراتور: تأیید پروندهٔ آماده در مرحله «بررسی کمیته نظارت».",
            "supervision_committee1: ورود — کارهای من — باز کردن پرونده.",
            "ثبت فرم پیشنهاد (ادامه) — «ثبت پیشنهاد».",
            "education_committee1: ورود — کارهای من — باز کردن پرونده.",
            "ثبت فرم حکم — «ادامه آموزش».",
            "student2: ورود — مشاهده باکس مهلت ۵ روز — آغاز دوباره درمان.",
            "اپراتور: تأیید وضعیت‌ها در هر مرحله.",
        ])
        self._section_bar("۱۱ — اگر مشکلی دیدید چه بنویسید؟")
        self._numbered_list([
            "کدام سناریو (۱، ۲ یا ۳) بود.",
            "با چه حسابی کار می‌کردید.",
            "در کدام مرحله بودید (نظارت، آموزش، دانشجو).",
            "چه انتظار داشتید و چه دیدید.",
            "از صفحه عکس بگیرید.",
        ])
        self._tip_box(
            "اگر «کارهای من» خالی است، احتمالاً پرونده هنوز ساخته نشده "
            "یا با حساب نقش اشتباه وارد شده‌اید — با مسئول پروژه هماهنگ کنید."
        )

    def limits(self) -> None:
        self._section_bar("۱۲ — انتظارات واقع‌بینانه (باگ نیست)")
        self._bullet_list([
            "مهلت ۳ و ۶ روزه ممکن است در محیط آزمایشی به‌صورت دستی نمایش داده شود "
            "و هشدار خودکار SLA هنوز فعال نباشد.",
            "نامهٔ رسمی قطع آموزش در محیط آزمایشی ممکن است فقط به‌صورت ثبت در پرونده باشد.",
            "پیامک واقعی ممکن است در محیط آزمایشی ارسال نشود؛ متن آن در صفحهٔ دانشجو نمایش داده می‌شود.",
            "برای آزمایش مسیر «قطع قطعی»، پروندهٔ جداگانه لازم است.",
        ])

    def final_form(self) -> None:
        self.pdf.add_page()
        self._section_bar("۱۳ — فرم نتیجهٔ نهایی تست")
        self._blank_lines("نام اپراتور:", 1)
        self._blank_lines("تاریخ:", 1)
        self._blank_lines("نام دانشجوی آزمایشی:", 1)
        p = self.pdf
        p.set_font("Vazir", "B", 9.5)
        p.cell(0, 7, _fa("محیط آزمایش:"), new_x="LMARGIN", new_y="NEXT")
        p.set_font("Vazir", "", 9.5)
        p.cell(
            0,
            7,
            _fa("[ ] روی کامپیوتر محلی   [ ] سرور آزمایشی   [ ] سرور واقعی"),
            new_x="LMARGIN",
            new_y="NEXT",
        )
        p.ln(3)
        self._body("نتیجهٔ هر بخش:", bold=True)
        rows = [
            "آماده‌سازی و ورود",
            "سناریوی ۱ — پیشنهاد کمیته نظارت",
            "سناریوی ۲ — حکم ادامه و مهلت دانشجو",
            "سناریوی ۳ — حکم قطع قطعی",
            "بررسی کلی UI و متن‌ها",
        ]
        with p.table(
            col_widths=(70, 22, 22, 22, 44),
            width=p.epw,
            text_align=Align.C,
            line_height=5.5,
            borders_layout=TableBordersLayout.ALL,
            cell_fill_color=(249, 250, 251),
            cell_fill_mode=TableCellFillMode.ROWS,
            padding=1.5,
        ) as table:
            hr = table.row()
            for h in [_fa("بخش"), _fa("قبول"), _fa("ناقص"), _fa("رد"), _fa("توضیح")]:
                hr.cell(h, style=_heading_style())
            for name in rows:
                r = table.row()
                r.cell(_fa(name), align=Align.R)
                for _ in range(3):
                    r.cell("")
                r.cell("")
        p.ln(4)
        self._body("نتیجهٔ کلی:", bold=True)
        p.cell(
            0,
            7,
            _fa("[ ] قبول — آمادهٔ استفاده   [ ] قبول مشروط   [ ] رد — نیاز به اصلاح"),
            new_x="LMARGIN",
            new_y="NEXT",
        )
        p.ln(3)
        self._blank_lines("باگ‌ها و یادداشت‌ها:", 5)
        self._blank_lines("امضا:", 1)


def main() -> int:
    guide = CommitteesReviewGuide()
    guide.cover()
    guide.intro()
    guide.prerequisites()
    guide.login_and_accounts()
    guide.ui_description()
    guide.operator_prep()
    guide.scenario(SCENARIO_SUPERVISION)
    guide.scenario(SCENARIO_EDUCATION_CONTINUE)
    guide.scenario(SCENARIO_EDUCATION_TERMINATE)
    guide.operator_verify()
    guide.quick_demo()
    guide.limits()
    guide.final_form()
    guide.save(OUT_PDF)
    print(f"PDF written: {OUT_PDF}")
    print(f"Size: {OUT_PDF.stat().st_size:,} bytes")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
