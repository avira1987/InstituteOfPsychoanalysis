#!/usr/bin/env python3
"""
راهنمای PDF آزمایش فرایند «برگزاری جلسه اضافی درمان آموزشی» (فرایند شماره ۴).

اجرا از ریشهٔ ریپو:
  python scripts/generate_extra_session_operator_guide_pdf.py

خروجی:
  docs/راهنمای_آزمایش_جلسه_اضافی_درمان.pdf
"""
from __future__ import annotations

import sys
from pathlib import Path

if sys.platform == "win32" and hasattr(sys.stdout, "reconfigure"):
    try:
        sys.stdout.reconfigure(encoding="utf-8")
    except Exception:
        pass

ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(ROOT))

import jdatetime
from fpdf.enums import Align, TableBordersLayout, TableCellFillMode, VAlign
from fpdf.fonts import FontFace

from scripts.generate_ui_tester_guide_pdf import (
    PdfBuilder,
    _fa,
    _heading_style,
    _MARGIN,
    _BODY,
    _SECTION,
    _TITLE,
    _SMALL,
    _TINY,
    _COLOR_STRIPE,
    _register_fonts,
    GuidePDF,
)

OUT_PDF = ROOT / "docs" / "راهنمای_آزمایش_جلسه_اضافی_درمان.pdf"

ACCOUNTS = [
    ("دانشجو (دارای درمان فعال)", "student1", "demo123"),
    ("درمانگر آموزشی", "therapist1", "demo123"),
]

SCENARIOS: list[dict] = [
    {
        "letter": "الف",
        "title": "مسیر اصلی — تأیید و پرداخت",
        "what": (
            "دانشجو یک جلسهٔ اضافی درخواست می‌کند؛ درمانگر زمان را می‌پذیرد؛ "
            "دانشجو هزینه را می‌پردازد؛ جلسه ثبت می‌شود و پس از برگزاری، "
            "درمانگر آن را «برگزار شد» علامت می‌زند."
        ),
        "steps": [
            "با حساب student1 وارد پورتال دانشجو شوید.",
            "به تب «فرایندها» بروید. اگر دکمهٔ «جلسه اضافی درمان» در دسترسی سریع "
            "فعال است، از همان استفاده کنید؛ وگرنه از فهرست فرایندها «برگزاری جلسه "
            "اضافی درمان آموزشی» را شروع کنید.",
            "تاریخ پیشنهادی را از تقویم شمسی انتخاب کنید و ساعت را وارد کنید. "
            "در صورت تمایل توضیح کوتاه بنویسید.",
            "دکمهٔ «ثبت اطلاعات این مرحله» را بزنید.",
            "دکمهٔ «ادامه و ثبت مرحله» (یا مشابه آن) را بزنید تا درخواست "
            "برای درمانگر ارسال شود.",
            "خارج شوید. با therapist1 وارد پورتال درمانگر شوید.",
            "تب «کارهای من» یا «منتظر اقدام» را باز کنید. پروندهٔ همان دانشجو "
            "با عنوان جلسه اضافی را پیدا و باز کنید.",
            "در بخش «پرونده و سابقه» تاریخ و ساعت پیشنهادی دانشجو را ببینید.",
            "دکمهٔ «درمانگر زمان پیشنهادی را تایید کرد — هدایت دانشجو به پرداخت» "
            "را بزنید.",
            "دوباره با student1 وارد شوید و همان فرایند را باز کنید.",
            "باید مرحلهٔ «پرداخت جلسه اضافی» را ببینید و بخش پرداخت "
            "(درگاه آزمایشی) روی صفحه باشد.",
            "پرداخت را انجام دهید. پس از بازگشت از درگاه، صفحه را یک‌بار "
            "تازه کنید (F5).",
            "باید وضعیت «جلسه اضافی تایید و ثبت شد» دیده شود و در پرونده "
            "تاریخ/ساعت توافق‌شده و در صورت وجود لینک جلسه نمایش داده شود.",
            "با therapist1 وارد شوید؛ همان پرونده را باز کنید و دکمهٔ "
            "«جلسه برگزار شد» را بزنید.",
            "وضعیت باید به «جلسه اضافی برگزار شد» برسد.",
        ],
        "expect": [
            "دانشجو بدون گیرکردن از درخواست تا پرداخت و ثبت جلسه برود.",
            "درمانگر درخواست را در صندوق کار ببیند و سه گزینهٔ تصمیم داشته باشد.",
            "پس از پرداخت، نیازی به زدن دکمهٔ دستی توسط دانشجو برای «تأیید پرداخت» نیست.",
            "پس از «برگزار شد»، فرایند بسته شود.",
        ],
        "checks": [
            "شروع فرایند از پورتال دانشجو",
            "انتخاب تاریخ شمسی و ساعت",
            "ارسال درخواست به درمانگر",
            "دیدن درخواست در پورتال درمانگر",
            "تأیید زمان توسط درمانگر",
            "نمایش بخش پرداخت برای دانشجو",
            "پرداخت آزمایشی و تغییر وضعیت",
            "ثبت «برگزار شد» توسط درمانگر",
            "متن یا دکمهٔ گیج‌کننده",
        ],
    },
    {
        "letter": "ب",
        "title": "پیشنهاد زمان جایگزین توسط درمانگر",
        "what": (
            "درمانگر زمان دانشجو را نمی‌پذیرد و زمان دیگری پیشنهاد می‌دهد. "
            "دانشجو یا همان زمان را می‌پذیرد یا زمان جدیدی پیشنهاد می‌دهد."
        ),
        "steps": [
            "مثل سناریوی الف تا مرحلهٔ بررسی درمانگر پیش بروید.",
            "به‌جای تأیید، تاریخ و ساعت جایگزین را در فرم «پیشنهاد زمان جایگزین» "
            "انتخاب کنید (تقویم شمسی + ساعت).",
            "دکمهٔ «درمانگر زمان را رد و زمان جایگزین پیشنهاد کرد» را بزنید.",
            "با student1 وارد شوید. وضعیت باید «پاسخ دانشجو به پیشنهاد جایگزین» باشد.",
            "در پرونده، تاریخ/ساعت پیشنهادی درمانگر را ببینید.",
            "مسیر ۱ — پذیرش: تاریخ و ساعت پیشنهادی را تأیید کنید، ثبت کنید، "
            "و «ادامه» بزنید — سپس باید به پرداخت برسید.",
            "مسیر ۲ — زمان جدید: به‌جای تأیید، تاریخ و ساعت دیگری انتخاب کنید "
            "و «ارسال زمان جدید» را بزنید — سپس باید دوباره به درمانگر برگردد.",
        ],
        "expect": [
            "پس از پیشنهاد جایگزین، دانشجو زمان درمانگر را در صفحه ببیند.",
            "پذیرش: رفتن به پرداخت؛ زمان جدید: بازگشت به بررسی درمانگر.",
        ],
        "checks": [
            "فرم پیشنهاد جایگزین در پورتال درمانگر",
            "نمایش زمان پیشنهادی برای دانشجو",
            "پذیرش و رفتن به پرداخت",
            "یا: ارسال زمان جدید و بازگشت به درمانگر",
            "متن یا دکمهٔ گیج‌کننده",
        ],
    },
    {
        "letter": "ج",
        "title": "رد درخواست — درمانگر امکان برگزاری ندارد",
        "what": "درمانگر اعلام می‌کند اصلاً نمی‌تواند جلسه اضافی برگزار کند.",
        "steps": [
            "درخواست جدید ثبت کنید (یا از پروندهٔ نیمه‌کار قبلی استفاده نکنید).",
            "با therapist1 پرونده را باز کنید.",
            "دکمهٔ «درمانگر امکان برگزاری ندارد» را بزنید.",
            "با student1 وارد شوید. وضعیت باید «درخواست رد شد» باشد.",
        ],
        "expect": [
            "فرایند بسته شود؛ دانشجو پیام واضح ببیند که درخواست پذیرفته نشده.",
        ],
        "checks": [
            "رد توسط درمانگر",
            "وضعیت نهایی «درخواست رد شد» برای دانشجو",
            "متن یا دکمهٔ گیج‌کننده",
        ],
    },
]


class ExtraSessionGuideBuilder(PdfBuilder):
    def cover(self) -> None:
        p = self.pdf
        p.add_page()
        p.set_font("Vazir", "B", _TITLE)
        p.ln(8)
        p.cell(
            0,
            12,
            _fa("راهنمای آزمایش و استفاده"),
            align="C",
            new_x="LMARGIN",
            new_y="NEXT",
        )
        p.set_font("Vazir", "B", 13)
        p.cell(
            0,
            10,
            _fa("فرایند شماره ۴ — برگزاری جلسه اضافی درمان آموزشی"),
            align="C",
            new_x="LMARGIN",
            new_y="NEXT",
        )
        p.ln(6)
        p.set_font("Vazir", "", _BODY)
        p.multi_cell(
            0,
            6,
            _fa(
                "این کتابچه برای کاربر آزمایشی (اپراتور، پذیرش، یا ناظر کیفیت) "
                "نوشته شده است تا بدون نیاز به دانش فنی، مراحل این فرایند را "
                "در سامانه امتحان کند و مطمئن شود همهٔ بخش‌ها درست کار می‌کنند."
            ),
            align="R",
        )
        p.ln(5)
        for label, blank in [
            ("نام آزمایش‌کننده", "________________________"),
            ("تاریخ آزمایش", "________________________"),
            ("آدرس سایت", "________________________"),
        ]:
            p.cell(0, 8, _fa(f"{label}: {blank}"), align="R", new_x="LMARGIN", new_y="NEXT")

    def intro(self) -> None:
        self.pdf.add_page()
        self._section_bar("این فرایند چیست؟")
        self._body(
            "گاهی دانشجو علاوه بر جلسات هفتگی معمول، یک جلسهٔ فوق‌العاده "
            "با درمانگر آموزشی خود می‌خواهد. در این فرایند:"
        )
        self._bullet_list([
            "دانشجو روز و ساعت پیشنهادی را انتخاب می‌کند.",
            "درمانگر می‌پذیرد، زمان دیگری پیشنهاد می‌دهد، یا اعلام می‌کند "
            "امکان برگزاری نیست.",
            "در صورت توافق، دانشجو هزینهٔ جلسه را می‌پردازد.",
            "جلسه در سامانه ثبت می‌شود و پس از برگزاری، درمانگر آن را "
            "«برگزار شد» علامت می‌زند.",
        ])
        self._section_bar("چه کسانی درگیرند؟")
        self._bullet_list([
            "دانشجو: ثبت درخواست، پاسخ به پیشنهاد جایگزین، پرداخت.",
            "درمانگر آموزشی: بررسی زمان، تأیید، پیشنهاد جایگزین، یا رد؛ "
            "و در پایان ثبت «برگزار شد».",
        ])
        self._section_bar("پیش‌نیاز — قبل از شروع آزمایش")
        self._body(
            "این فرایند فقط برای دانشجویی فعال است که «درمان آموزشی» "
            "در پروندهٔ او قبلاً آغاز شده باشد. اگر دکمهٔ «جلسه اضافی درمان» "
            "غیرفعال است یا پیام «پس از آغاز درمان…» می‌بینید، ابتدا باید "
            "فرایند «آغاز درمان آموزشی» برای همان دانشجو تکمیل شده باشد."
        )
        self._tip_box(
            "در محیط آزمایشی معمولاً از حساب student1 استفاده می‌شود که "
            "درمان برای او فعال است. اگر مطمئن نیستید، از مسئول پروژه بپرسید."
        )

    def login_and_accounts(self) -> None:
        self.pdf.add_page()
        self._section_bar("ورود به سامانه")
        self._numbered_list([
            "مرورگر را باز کنید و به صفحهٔ ورود بروید "
            "(آدرس را از مسئول پروژه بگیرید؛ در آزمایش محلی معمولاً "
            "http://localhost:3000/login).",
            "روی «ورود با رمز عبور» بزنید.",
            "سؤال ریاضی را جواب دهید.",
            "نام کاربری و رمز را وارد کنید و «ورود» بزنید.",
            "برای عوض کردن نقش: «خروج» و سپس دوباره ورود با حساب دیگر.",
        ])
        self._section_bar("حساب‌های پیشنهادی برای این آزمایش")
        p = self.pdf
        with p.table(
            col_widths=(62, 58, 28),
            width=p.epw,
            text_align=Align.R,
            line_height=5.5,
            borders_layout=TableBordersLayout.ALL,
            cell_fill_color=_COLOR_STRIPE,
            cell_fill_mode=TableCellFillMode.ROWS,
            padding=1.5,
        ) as table:
            hr = table.row()
            for h in [_fa("نقش"), _fa("نام کاربری"), _fa("رمز")]:
                hr.cell(h, align=Align.C, style=_heading_style())
            for role, user, pw in ACCOUNTS:
                row = table.row()
                row.cell(_fa(role))
                row.cell(user, align=Align.C)
                row.cell(pw, align=Align.C)
        p.ln(4)

    def where_to_go(self) -> None:
        self.pdf.add_page()
        self._section_bar("کجا در سامانه بروم؟")
        self._body("پورتال دانشجو", bold=True)
        self._bullet_list([
            "بعد از ورود با حساب دانشجو، صفحهٔ اصلی دانشجو باز می‌شود.",
            "تب «فرایندها»: فهرست فرایندهای فعال و شروع فرایند جدید.",
            "دسترسی سریع: دکمهٔ «جلسه اضافی درمان» (اگر فعال باشد).",
            "روی نام فرایند کلیک کنید تا جزئیات مرحلهٔ فعلی، فرم و دکمه‌ها "
            "نمایش داده شود.",
        ])
        self._body("پورتال درمانگر", bold=True)
        self._bullet_list([
            "بعد از ورود با therapist1، تب «کارهای من» یا «منتظر اقدام».",
            "درخواست‌های جلسه اضافی در فهرست کارهای منتظر ظاهر می‌شوند.",
            "با کلیک روی پرونده، جزئیات، تاریخ پیشنهادی دانشجو و دکمه‌های "
            "تصمیم را می‌بینید.",
        ])
        self._section_bar("مراحل فرایند — نمای کلی")
        stages = [
            ("۱", "ثبت درخواست", "دانشجو", "انتخاب تاریخ شمسی و ساعت"),
            ("۲", "بررسی درمانگر", "درمانگر", "تأیید / پیشنهاد جایگزین / عدم امکان"),
            ("۳", "پاسخ دانشجو", "دانشجو", "فقط اگر درمانگر زمان جایگزین داده باشد"),
            ("۴", "پرداخت", "دانشجو", "پرداخت هزینهٔ جلسه از همین صفحه"),
            ("۵", "ثبت جلسه", "سامانه", "خودکار پس از پرداخت موفق"),
            ("۶", "برگزاری", "درمانگر", "دکمهٔ «جلسه برگزار شد»"),
        ]
        p = self.pdf
        with p.table(
            col_widths=(12, 42, 28, 98),
            width=p.epw,
            text_align=Align.R,
            line_height=5.5,
            borders_layout=TableBordersLayout.ALL,
            cell_fill_color=_COLOR_STRIPE,
            cell_fill_mode=TableCellFillMode.ROWS,
            padding=1.5,
        ) as table:
            hr = table.row()
            for h in [_fa("ردیف"), _fa("مرحله"), _fa("مسئول"), _fa("کار شما")]:
                hr.cell(h, align=Align.C, style=_heading_style())
            for num, stage, who, task in stages:
                row = table.row()
                row.cell(num, align=Align.C)
                row.cell(_fa(stage))
                row.cell(_fa(who))
                row.cell(_fa(task))
        p.ln(4)

    def tips(self) -> None:
        self.pdf.add_page()
        self._section_bar("نکات مهم هنگام آزمایش")
        self._bullet_list([
            "همیشه اول «ثبت اطلاعات این مرحله» را بزنید، بعد «ادامه» یا "
            "دکمهٔ تصمیم.",
            "تاریخ را از تقویم شمسی انتخاب کنید؛ نیازی به نوشتن تاریخ "
            "میلادی نیست.",
            "در مرحلهٔ پرداخت، دکمهٔ «ادامه» برای دانشجو نیست — فقط "
            "بخش پرداخت را تکمیل کنید.",
            "بعد از پرداخت، یک‌بار صفحه را تازه کنید تا وضعیت جدید "
            "دیده شود.",
            "در محیط آزمایشی پرداخت واقعی نیست؛ فقط مسیر درگاه را "
            "طی می‌کنید.",
            "پیامک‌ها ممکن است در سامانه به‌صورت «شبیه‌سازی پیامک» "
            "نمایش داده شوند.",
            "اگر فرایند فعال از قبل دارید، از همان ادامه دهید؛ "
            "شروع دوباره ممکن نیست.",
        ])
        self._section_bar("چیزهایی که طبیعی است (باگ نیست)")
        self._bullet_list([
            "مرحلهٔ «ثبت جلسه» بدون دکمه — خودکار انجام می‌شود.",
            "دانشجو در مرحلهٔ «منتظر درمانگر» دکمهٔ ادامه ندارد.",
            "مهلت پرداخت محدود است؛ اگر بگذرد، فرایند لغو می‌شود.",
        ])

    def scenario(self, sc: dict) -> None:
        self.pdf.add_page()
        self._section_bar(f"سناریوی {sc['letter']} — {sc['title']}")
        self._body("هدف این سناریو", bold=True)
        self._body(sc["what"])
        self._body("قدم‌به‌قدم:", bold=True)
        self._numbered_list(sc["steps"])
        self._body("باید چه ببینید؟", bold=True)
        self._bullet_list(sc["expect"])
        self._body("چک‌لیست — بعد از انجام علامت بزنید:", bold=True)
        self._check_table(sc["checks"])
        self._blank_lines("مشکل یا باگ (چه انتظار داشتید / چه شد):", 2)
        self._priority_line()

    def operator_checklist(self) -> None:
        self.pdf.add_page()
        self._section_bar("چک‌لیست نهایی اپراتور")
        self._body(
            "پس از انجام هر سه سناریو، این موارد را یک‌بار دیگر مرور کنید:"
        )
        final_checks = [
            "آیا متن راهنما روی صفحه (جعبهٔ آبی/زرد) برای هر مرحله واضح بود؟",
            "آیا تاریخ شمسی درست نمایش داده شد (هم هنگام انتخاب، هم در پرونده)؟",
            "آیا درمانگر سه گزینهٔ تصمیم را دید؟",
            "آیا پس از تأیید، بخش پرداخت برای دانشجو ظاهر شد؟",
            "آیا پس از پرداخت، وضعیت بدون کار اضافهٔ دانشجو عوض شد؟",
            "آیا لینک یا خلاصهٔ جلسه در پرونده دیده شد؟",
            "آیا «برگزار شد» فرایند را بست؟",
            "آیا مسیر پیشنهاد جایگزین درست کار کرد؟",
            "آیا مسیر «عدم امکان» درست بسته شد؟",
        ]
        self._check_table(final_checks)
        self._section_bar("گزارش مشکل")
        self._body(
            "برای هر مورد مشکل‌دار بنویسید: در کدام مرحله، با چه حسابی، "
            "چه انتظار داشتید، چه اتفاق افتاد. در صورت امکان از صفحه "
            "عکس بگیرید و برای مسئول پروژه بفرستید."
        )
        self._blank_lines("جمع‌بندی کلی (۱ = خیلی ضعیف — ۵ = عالی):", 1)
        self._body("امتیاز کلی تجربهٔ کاربری این فرایند:  [ ] ۱  [ ] ۲  [ ] ۳  [ ] ۴  [ ] ۵")


def main() -> int:
    builder = ExtraSessionGuideBuilder()
    builder.cover()
    builder.intro()
    builder.login_and_accounts()
    builder.where_to_go()
    builder.tips()
    for sc in SCENARIOS:
        builder.scenario(sc)
    builder.operator_checklist()
    builder.save(OUT_PDF)
    print(f"PDF written: {OUT_PDF}")
    print(f"Size: {OUT_PDF.stat().st_size:,} bytes")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
